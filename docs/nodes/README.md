# BornSystem Nodes

This folder contains the execution node definitions for BornSystem.

- `kali/` — Kali Linux node image and agent implementation
- `ubuntu/` — Ubuntu node image and agent implementation
- `windows/` — placeholder for Windows agents

Each node runs a Python agent and can be extended with platform-specific tooling.
