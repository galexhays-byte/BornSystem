# BornSystem Storage Layout

This folder contains the storage repositories for field binaries, scripts, configs, and logs.

- `/binaries` — compiled tools and ready-to-run runtime binaries
- `/scripts` — automation scripts for field devices and nodes
- `/configs` — configuration dictionaries, payload lists, and signature data
- `/logs` — locally generated logs from field task runs
