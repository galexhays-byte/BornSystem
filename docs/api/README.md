# BornSystem API Documentation

The API is served from the backend service and currently provides:

- `GET /health` — service health check
- `GET /api/example` — sample endpoint
- `GET /api/status` — system status summary
- `GET /api/nodes/list` — list registered nodes
- `POST /api/nodes/execute` — execute a node action
