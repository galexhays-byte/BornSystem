# BornSystem Architecture

This document describes the high-level architecture for the BornSystem project.

- `backend/` contains the FastAPI orchestration core.
- `frontend/` contains the user interface assets and static page shell.
- `nodes/` contains containerized execution agents for different operating systems.
- `docker/` contains the multi-container orchestration toolchain.
- `workflows/` contains workflow definitions and CI templates.
